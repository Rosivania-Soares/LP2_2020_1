_c0e‍.x([["default",()=>Cliente]]);/**
 * Exporta (deixa pública) a
 * classe Cliente com a forma
 * mais moderna (ES6)
 */
               class Cliente{

    constructor(nome, endereco, cpf, telefone){ 
        this.nome = nome;
        this.endereco = endereco;
        this.cpf = cpf;
        this.telefone = telefone;
    }
}

/**
 * Exporta (deixa pública) a
 * classe Cliente na forma
 * antiga (old school) do JS
 */
//module.exports = Cliente;